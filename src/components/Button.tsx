import { TouchableOpacity, Text } from "react-native"

const Button = () => {
  return (
    <TouchableOpacity>
        <Text className="text-white border border-white p-2 rounded-md text-center">
            Button
        </Text>
    </TouchableOpacity>
  )
}

export default Button