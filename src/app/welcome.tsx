import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

export default function Welcome() {
  // This is the welcome tab component.
  return ( 
    <SafeAreaView>
        <Text>Welcome</Text>
    </SafeAreaView>
  );
}